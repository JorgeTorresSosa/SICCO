<Window x:Class="WPFSICCO.PantallaInicio"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFSICCO"
        mc:Ignorable="d"
        Title="PantallaInicio" Height="680" Width="800" WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen"
        xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Height="680" VerticalAlignment="Top" Margin="0,0,0,-100">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="Transparent" MouseLeftButtonDown="Border_MouseLeftButtonDown">
            <Grid>
                <MaterialDesign:ColorZone Mode="PrimaryDark" Background="#FF009688" Height="50" MinWidth="60" Margin="0,0,0,-20" RenderTransformOrigin="0.5,0.5">
                    <StackPanel HorizontalAlignment="Right">
                        <Button Width="100" Height="50" MaterialDesign:ShadowAssist.ShadowDepth="Depth3" Padding="0" Click="Button_Click">
                            <MaterialDesign:PackIcon Kind="WindowClose"/>
                        </Button>
                    </StackPanel>
                </MaterialDesign:ColorZone>
            </Grid>
        </Border>
        <Grid Width="110" HorizontalAlignment="Left" Background="#FF004D40" Opacity="0.65" Height="615" VerticalAlignment="Bottom" Grid.Row="1" >

            <Button x:Name="Boton" Height="Auto" Margin="0,-65,0,594" Background="#FFC51162" />
        </Grid>

    </Grid>
</Window>
